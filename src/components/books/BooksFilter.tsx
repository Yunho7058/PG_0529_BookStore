import React from 'react';
import styled from 'styled-components';

const BooksFilter = () => {
  return <BooksFilterStyle></BooksFilterStyle>;
};

const BooksFilterStyle = styled.div``;

export default BooksFilter;
