import React from 'react';
import styled from 'styled-components';

const BooksView = () => {
  return <BooksViewStyle></BooksViewStyle>;
};

const BooksViewStyle = styled.div``;

export default BooksView;
